ANTHROPIC_API_KEY=your-key-here
OPENAI_API_KEY=your-key-here

# Admin credentials for `gollm purge` (wipes synthetic data between runs).
# Read by name from the app config's admin.identifier_env / admin.password_env.
VOTERBLOC_ADMIN_EMAIL=admin@example.com
VOTERBLOC_ADMIN_PASSWORD=your-admin-password
